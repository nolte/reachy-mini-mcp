# Development and test-only dependencies.
# Install independently from requirements.txt — never via `-r requirements.txt`.
ruff>=0.8.4
pytest>=8.3.0
pytest-asyncio>=0.24.0
mkdocs>=1.6.0
mkdocs-material>=9.5.0
